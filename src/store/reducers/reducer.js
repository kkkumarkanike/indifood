const initialState = {
    isSignIn : false,
    email : null,
    token : null,
    userId : null,
    expireTime : null
}

const reducer = (state=initialState , action) =>{
    return state;
}

export default reducer;